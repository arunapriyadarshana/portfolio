import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About",
  description: "Aruna Priyadarshana's About",
};

const page = () => {
  return <div>about</div>;
};

export default page;
